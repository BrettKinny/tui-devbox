#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
if ! command -v node >/dev/null 2>&1; then
	echo 'sqrbx-agent: Node.js 22 or newer is required on the host.' >&2
	exit 1
fi
exec node "$ROOT/agent/cli.mjs" "$@"
